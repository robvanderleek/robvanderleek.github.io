{
  "name": "robvanderleek.github.io",
  "private": true,
  "scripts": {
    "start": "next",
    "dev": "next",
    "build": "next build",
    "export": "yarn run build && next export && touch out/.nojekyll",
    "deploy": "yarn export && gh-pages -t -b master -d out",
    "start-prod": "next start"
  },
  "dependencies": {
    "next": "^9.1.6",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "styled-components": "^4.4.1"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
